{% extends 'base.html.twig' %}

{% block title %}{{'New Counter'|trans}}{% endblock %}

{% block body %}
    <h1>{{'Create new Counter'|trans}}</h1>

    {{ include('counter/_form.html.twig') }}

    <a href="{{ path('counter_index') }}">{{'back to list'|trans}}</a>
{% endblock %}
