<form method="post" action="{{ path('note_simulation_delete', {'id': note_simulation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note_simulation.id) }}">
    <button class="btn btn-danger">Delete</button>
    
</form>
