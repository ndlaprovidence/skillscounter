<form method="post" action="{{ path('scorecard_delete', {'id': scorecard.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scorecard.id) }}">
    <button class="btn">{{'Delete'|trans}}</button>
</form>
